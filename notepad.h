#ifndef NOTEPAD_h
#define NOTEPAD_h

#include <iostream>
#include <fstream>
#include <string>

using namespace std;

void createNote(string fileName);
void showNote(string fileName);
void addNote(string fileName);

#endif
